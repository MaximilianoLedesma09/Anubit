// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // El site debe ser tu nombre de usuario, no la dirección del repositorio
  site: 'https://MaximilianoLedesma09.github.io',
  // El base debe ser el nombre del repositorio, con una barra al inicio
  base: '/Anubit',
});


